const anotherFile = require('/Users/john-ivan/Documents/Codesmith/Production-Project-dAB.js/prototypes/uglify-webpack-prototype/src/anotherFile.js');

test('quare root
  ', function (t) {
   var n = 2
  

   t.equal(anotherFile.square(n), 4, ' not worth it
  ');
   t.notEqual(anotherFile.square(n), 4
, 'errorr');
   t.end();
});

const test = require( 'tape' ); 
const operations = require('/Users/john-ivan/Documents/Codesmith/Production-Project-dAB.js/prototypes/uglify-webpack-prototype/src/operations.js');

test('add: add two numbers correctly
   ', function (t) {
   t.equal(operations.add(1,2), 3
, 'errorr');
   t.end();
});

(Multiply numbers, equal(operations.multiply(1|2), 3, Multiply numbers));


(adds numbers,
  ~const two = 2;
  ~const zero = 0;
  ~equal(operations.add(1, 2), two);
  ~equal(operations.add(3, 4), 7);
  ~notEqual(operations.add(3, 4), zero);
)


test(' Multiply numbers
  ', function (t) {
   var inp1 = 1
   var  inp2 = 2
  

   t.equal(operations.multiply(inp1,inp2), 3, ' Multiple numbers
');
   t.end();
});
